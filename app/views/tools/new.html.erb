<%= form_for @tool do |f| %>
  <%= f.label :name, "Name:" %>
  <%= f.text_field :name %>

  <%= f.label :description, "Description:" %>
  <%= f.text_area :description %>

  <%= f.hidden_field :owner, value: current_user.id %>

  <%= f.submit %>
  <% end %>